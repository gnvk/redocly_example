get:
  summary: Get a trade
  tags:
    - Stock
  parameters:
    - $ref: "../../parameters/stock/symbol.yaml"
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            $ref: "../../schemas/stock/trade.yaml"
    "400":
      $ref: "../../responses/400.yaml"

